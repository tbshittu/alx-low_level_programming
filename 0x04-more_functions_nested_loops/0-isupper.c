#include "main.h"

/**
 * _isupper - Check if a letter is uppercase
 * @b: The number to be cyhecked
 * Return: 1 for uppercase and 0 for anything else
 */

int _isupport(int x)

{
	if (b >= 65 && b <= 90)
	{
	return (1);
	}
	return (0);
}
